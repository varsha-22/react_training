// import PrivateRoute from '../../routes/PrivateRoute';

// const getDateFormatted = createdAt => {
//   let formatDate = moment(createdAt).format("dddd MMMM Do YYYY, h:mm:ss a");
//   return formatDate;
// };

// const handleClick = id => {
//   trainees.map((trainee, i) =>
//     id === trainee.id ? (
//       <TraineeDetails
//         key={i}
//         name={trainee.name}
//         createdAt={getDateFormatted(trainee.createdAt)}
//         email={trainee.email}
//       />
//     ) : (
//       <h2>No Link Found</h2>
//     )
//   );
// };
